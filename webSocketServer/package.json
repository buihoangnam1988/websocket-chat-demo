{
  "name": "websocketserver",
  "version": "1.0.0",
  "description": "My first websocket app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "PORT=8002 node index.js",
    "start_pc": "set PORT=8002 && node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "http": "^0.0.1-security",
    "websocket": "^1.0.35"
  }
}
